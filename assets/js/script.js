document.addEventListener("DOMContentLoaded", function () {
  const dateTime = document.getElementById("dateTime"); function showDateTime() { if (dateTime) { const now = new Date(); dateTime.textContent = now.toLocaleDateString() + " | " + now.toLocaleTimeString() } } showDateTime(); setInterval(showDateTime, 1000);

  const quiz = document.getElementById("plantQuiz"); if (quiz) { quiz.addEventListener("submit", function (e) { e.preventDefault(); const sunlight = document.getElementById("sunlight").value; const care = document.getElementById("careLevel").value; const pets = document.getElementById("pets").value; let plant = "Snake Plant"; if (pets === "yes") plant = "Spider Plant"; else if (sunlight === "bright" && care === "high") plant = "Monstera"; else if (sunlight === "medium") plant = "Pothos"; else if (sunlight === "low") plant = "ZZ Plant"; document.getElementById("quizResult").innerHTML = "<div class='alert alert-success'><strong>Your recommended plant is " + plant + ".</strong></div>" }) }

  const membershipButton = document.getElementById("membershipButton"); if (membershipButton) { membershipButton.addEventListener("click", function () { const content = document.getElementById("membershipContent"); content.classList.toggle("d-none"); membershipButton.textContent = content.classList.contains("d-none") ? "Show" : "Hide" }) }

  const cartButton = document.getElementById("cartButton"); if (cartButton) { cartButton.addEventListener("click", function () { const size = document.getElementById("plantSize").value; const addon = document.getElementById("addon").value; document.getElementById("cartMessage").innerHTML = "<div class='alert alert-success'>Monstera (" + size + ") added to cart with " + addon + ".</div>" }) }

  const tipForm = document.getElementById("tipForm"); if (tipForm) { tipForm.addEventListener("submit", function (e) { e.preventDefault(); const name = document.getElementById("tipName").value; const tip = document.getElementById("tipText").value; document.getElementById("tipResult").innerHTML = "<div class='alert alert-success'><strong>" + name + ":</strong> " + tip + "</div>"; tipForm.reset() }) }

  const contactForm = document.getElementById("contactForm"); if (contactForm) { contactForm.addEventListener("submit", function (e) { e.preventDefault(); if (!contactForm.checkValidity()) { contactForm.reportValidity(); return } if (confirm("Are you sure you want to submit this message?")) { alert("Thank you! Your message has been submitted successfully."); contactForm.reset() } }) }

  const chatButton = document.getElementById("chatButton"); const chatBox = document.getElementById("chatBox"); const sendChat = document.getElementById("sendChat"); const chatInput = document.getElementById("chatInput"); const chatMessages = document.getElementById("chatMessages"); if (chatButton) { chatButton.addEventListener("click", function () { chatBox.classList.toggle("d-none"); chatButton.textContent = chatBox.classList.contains("d-none") ? "Open Live Chat" : "Close Live Chat" }) } if (sendChat) { sendChat.addEventListener("click", function () { if (chatInput.value.trim() === "") return; chatMessages.innerHTML += "<p><strong>You:</strong> " + chatInput.value + "</p><p><strong>Specialist:</strong> Check your plant's light, soil moisture and drainage. Avoid overwatering.</p>"; chatInput.value = "" }) }
});